/**
 * The CSV drop target with the radar/concentric-circle background, upload icon,
 * "Drop your CSV here" copy, helper text, Sample CSV button + Upload guide link.
 *
 * This is the CORE interaction: dropping (or selecting) a CSV opens the
 * "Submit new enrichment" config modal. That modal is built in a later step.
 * STUB for now.
 */
export function UploadDropzone() {
  return (
    <section data-slot="upload-dropzone">
      {/* TODO: radar background, upload icon, CTA copy, Sample CSV, Upload guide.
          On drop/select -> open enrichment config modal. */}
    </section>
  );
}
