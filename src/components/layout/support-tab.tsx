/**
 * Vertical "Customer Support" tab pinned to the right edge of the viewport.
 * STUB — styled in a later step.
 */
export function SupportTab() {
  return (
    <div
      data-slot="support-tab"
      className="fixed right-0 top-1/2 -translate-y-1/2"
    >
      {/* TODO: vertical "Customer Support" pill */}
    </div>
  );
}
