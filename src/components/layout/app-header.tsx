/**
 * Top bar: sidebar toggle, credits meter, Add credits, command search (⌘K), API Keys.
 * STUB — built in a later step. Sidebar toggle + command palette are the interactive bits.
 */
export function AppHeader() {
  return (
    <header
      data-slot="app-header"
      className="flex h-14 items-center gap-3 border-b border-border px-4"
    >
      {/* TODO: SidebarToggle, CreditsMeter, AddCreditsButton, CommandSearch, ApiKeysButton */}
    </header>
  );
}
