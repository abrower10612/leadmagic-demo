/**
 * Left navigation: brand, primary nav, collapsible Signals/Companies groups, footer user card.
 * STUB — will be built with the shadcn `sidebar` primitive (with collapse support) in a later step.
 */
export function AppSidebar() {
  return (
    <aside
      data-slot="app-sidebar"
      className="w-64 shrink-0 border-r border-border bg-sidebar"
    >
      {/* TODO: SidebarBrand, SidebarNav, SidebarGroup (Signals/Companies), SidebarFooter */}
    </aside>
  );
}
